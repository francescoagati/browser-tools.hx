import browser_tools.animations.DisplayObject.TransactionCheck;
import browser_tools.animations.fps.Loop;
import browser_tools.animations.fps.Printer;
import async_tools.Cps.*;

import js.Browser;
using browser_tools.HtmlTools;




class Test {

  public static function main() {
    //new Printer();
    //var container:browser_tools.animations.DisplayObject = 'animation'.byId();

    //var container = 'animation'.byId();

    //container.classes + 'a1' + 'a2' + 'a3' - 'b1' - 'b2';
    /container.classes == 'a1';
    //js.Browser.window.document.onerror = function(e) js.Browser.alert(e);

/*
    cont_exec({
      while(true) {
        var rt = @await browser_tools.animations.DisplayObject.TransactionCheck.check_for_property_change(container,left);
        'log'.byId() ^ '$rt';
        @await wait(500);
      }
    });
*/

  }





}
